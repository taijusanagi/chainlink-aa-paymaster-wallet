import { expect } from "chai";

describe("Unit Test for Capsule", function () {
  describe("Func:", function () {
    it("Should work", async function () {
      expect(true).to.eq(true);
    });
  });
});
